# Audio capture
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24,<2.0

# PortAudio backend — platform-specific:
#   Windows: pyaudiowpatch (adds WASAPI loopback devices for system audio)
#   macOS/Linux: vanilla pyaudio (internal-audio capture needs BlackHole on macOS)
pyaudiowpatch>=0.2.12; sys_platform == "win32"
pyaudio>=0.2.12; sys_platform == "darwin" or sys_platform == "linux"
# macOS source builds need PortAudio headers first: `brew install portaudio`

# Fingerprinting (dejavu requires MySQL + pyaudio; we only use dejavu's core)
mysql-connector-python>=8.0  # driver dejavu's MySQLDatabase expects
dejavu>=0.1.0           # audio fingerprinting library (worldveil/dejavu fork on PyPI)

# Metadata
mutagen>=1.47.0

# Rendering
pygame-ce>=2.4.0        # community-edition pygame (SDL2, multi-display aware)

# Debug UI
customtkinter>=5.2.0
Pillow>=10.0.0

# Bundled static ffmpeg binary (all platforms) — used as the tolerant-decode
# fallback for corrupt/truncated audio files. A system ffmpeg on PATH still
# takes priority; this guarantees the fallback works on machines without it.
imageio-ffmpeg>=0.5.1

# Optional: a system ffmpeg on PATH also works and takes priority.
#   Windows: `winget install Gyan.FFmpeg`
#   macOS:   `brew install ffmpeg`
